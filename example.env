# ──────────────────────────────────────────────────────
# .env
# ──────────────────────────────────────────────────────
# ── BlackBox SFTP credentials
SFTP_HOST=sftp.blackboxglobal.com
SFTP_PORT=22
SFTP_USER=your_username
SFTP_PASS=your_password
REMOTE_ROOT=/incoming/videos

# ── SMB roots (comma-separated list of valid batch dirs)
SMB_ROOTS=B:/Video/StockFootage/Batches,B:/Video/StockFootage/Curated

# ── Default output / curation dirs (relative to repo)
DEFAULT_OUTPUT_DIR=./metadata
DEFAULT_CURATION_DIR=./curated

# ── Local LLM & VTT model paths
# Point these at the exact files / folders you downloaded
LOCAL_LLM_PATH=D:/Models/llama2-7b-chat.Q4_K_M.gguf
LOCAL_VTT_PATH=D:/Models/blip2-opt-2.7b

# (Optional) If you ever want to override caption or BLIP fallback:
# BLIP_MODEL=Salesforce/blip2-opt-2.7b
# BLIP_FALLBACK=Salesforce/blip-image-captioning-base
# YOLO_MODEL=yolov8n.pt
# CLIP_MODEL=ViT-B/32
# CLIP_THRESHOLD=0.3
# ──────────────────────────────────────────────────────

# ──────────────────────────────────────────────────────
# ── Containerized Services (Optional)
# Postgres for Resolve
PG_HOST=192.168.0.21
PG_PORT=5432
PG_DB=resolveLibrary
PG_USER=davinci
PG_PASS=supersecret

# MinIO
MINIO_ENDPOINT=192.168.0.21:9000
MINIO_ACCESS=minio
MINIO_SECRET=minio123
MINIO_SECURE=false

# Weaviate
WEAVIATE_URL=http://192.168.0.21:8082
# ──────────────────────────────────────────────────────